# Lighter client uses httpx + websockets directly
# Hyperliquid client uses the official SDK (websockets kept for Lighter)
tweepy>=4.14.0
python-dotenv>=1.0.0
pyyaml>=6.0
httpx[socks]>=0.27.0       # [socks] extra pulls in socksio for SOCKS5 proxy support
aiohttp>=3.9.0
apscheduler>=3.10.0
websockets>=12.0
python-socks[asyncio]>=2.4.0  # SOCKS5 backend for websockets v16 proxy support
hyperliquid-python-sdk>=0.23.0
Pillow>=10.0.0
